version: "3.1"

intents:
  - ask_chatgpt

actions:
  - action_handle_query

slots:
  sender_id:
    type: text
    influence_conversation: false
    mappings: [] 


session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
